public static int size(Node node) {
    // write your code here
    if(node==null){
      return 0;
    }
    int size=0;
    size += size(node.left);
    size+= size(node.right);
    return size+1;
  }
