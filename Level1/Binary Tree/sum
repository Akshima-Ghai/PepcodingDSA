public static int sum(Node node) {
    // write your code here
    if(node == null){
      return 0;
    }
    int sum = 0;
    sum += sum(node.left);
    sum+=sum(node.right);
    return sum+node.data;
  }
